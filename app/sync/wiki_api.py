"""eqlwiki.com sync — M5 milestone. Stub until then.

Contract: run(ctx) crawls the wiki via its MediaWiki API (api.php), writing
quests/items/guides + sync_pages/raw_pages. All HTTP through ctx.fetch().
"""


def run(ctx):
    raise NotImplementedError('wiki sync arrives in milestone M5')

"""eqlegendstools.com sync — M6 milestone. Stub until then.

Contract: run(ctx) crawls sitemap.xml -> item/zone/category pages, writing
items/item_effects/effects + sync_pages/raw_pages. All HTTP through ctx.fetch()
(which hard-refuses the robots-disallowed /api/ path).
"""


def run(ctx):
    raise NotImplementedError('tools-site sync arrives in milestone M6')

"""Live log tailer — M4 milestone. Stub until then.

Contract: start() is called once from server startup. It must be safe to call
when no character/log exists. It owns state.live (see app/state.py).
"""


def start() -> None:
    pass
